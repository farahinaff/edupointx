services:
  - type: web
    name: edupointx-mobile
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT
    disks:
      - name: edupointx-data
        mountPath: /var/data
        sizeGB: 1
